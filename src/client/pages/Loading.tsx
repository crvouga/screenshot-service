import { Box, Typography } from "@mui/material";

export const LoadingPage = () => {
  return (
    <Box
      sx={{
        width: "100%",
        height: "100%",
        display: "grid",
        placeItems: "center",
      }}
    >
      <Typography variant="h1">📸</Typography>
    </Box>
  );
};
